# Encore configuration file
# This tells Encore to generate client types in the backend directory
# instead of the default root-level encore.gen directory.

gen:
  # Output directory for generated client code
  output: backend/encore.gen

# Exclude the frontend directory from Encore's build, as it's a separate
# Vite-built SPA that should not be compiled by Encore.
exclude:
  - frontend
